<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.admin.mapper.SysMapper">
    <select id="findUserPerms" parameterType="java.lang.Long" resultType="java.util.Map">
      SELECT p.* FROM tb_perm p JOIN tb_role_perm rp ON p.id=rp.perm_id WHERE rp.role_id  IN
       (SELECT ur.id FROM tb_user u JOIN tb_user_role ur ON u.id=ur.role_id WHERE  u.id=#{userid})
  </select>

</mapper>